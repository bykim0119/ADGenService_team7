apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: comfyui-models-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 25Gi
  storageClassName: standard-rwo
